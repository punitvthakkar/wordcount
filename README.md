# Word Counter

A minimal, single-file HTML word counter. No dependencies, no build step.

## Usage

Open `counter.html` in any browser. Paste or type text into the textarea. Stats update instantly.

## Metrics

- Word count
- Character count
- Character count excluding spaces
- Sentence count

## Notes

Works entirely offline. Nothing is sent anywhere.
